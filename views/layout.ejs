<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Line Balancing Analysis</title>
    
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    
    <script src="https://polyfill.io/v3/polyfill.min.js?features=es6"></script>
    <script id="MathJax-script" async src="https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-mml-chtml.js"></script>
    <script>
        window.MathJax = {
            tex: {
                inlineMath: [['$', '$'], ['\\(', '\\)']],
                displayMath: [['$$', '$$'], ['\\[', '\\]']]
            },
            svg: { fontCache: 'global' }
        };
    </script>

    <style>
        :root {
            --primary: #0f172a;
            --accent: #2563eb;
            --bg: #ffffff;
            --card-bg: #f8fafc;
            --border: #e2e8f0;
        }

        body { 
            font-family: 'Plus Jakarta Sans', sans-serif; 
            background-color: var(--bg); 
            color: var(--primary); 
            margin: 0; 
            line-height: 1.6;
        }

        nav { 
            padding: 1.5rem 10%; 
            display: flex; 
            justify-content: space-between; 
            align-items: center;
            background: rgba(255, 255, 255, 0.8);
            backdrop-filter: blur(10px);
            position: sticky;
            top: 0;
            z-index: 1000;
        }

        .nav-brand { font-weight: 800; font-size: 1.2rem; letter-spacing: -0.03em; }
        .nav-links a { text-decoration: none; color: #64748b; font-weight: 600; font-size: 0.9rem; margin-left: 2rem; transition: 0.3s; }
        .nav-links a:hover { color: var(--accent); }

        .container { max-width: 1200px; margin: 0 auto; padding: 0 5%; }

        footer { 
            margin-top: 100px; 
            padding: 50px 0; 
            background: var(--card-bg);
            text-align: center;
            border-top: 1px solid var(--border);
        }

        .team-tag { font-size: 0.7rem; font-weight: 800; text-transform: uppercase; letter-spacing: 0.15em; color: #94a3b8; margin-bottom: 10px; display: block; }
    </style>
</head>
<body>
    <nav>
        <div class="nav-brand">Line Balancing Analysis</div>
        <div class="nav-links">
            <a href="/">Beranda</a>
            <a href="/input">Analisis Baru</a>
        </div>
    </nav>

    <div class="container">
        <%- body %>
    </div>

    <footer>
        <span class="team-tag">Project Team</span>
        <p style="font-weight: 600; color: #475569; font-size: 0.95rem;">
            Muhammad Fiqri Alamsyah (068) &nbsp;&nbsp;|&nbsp;&nbsp; Fedora Raffi Praspa (107)
        </p>
    </footer>
</body>
</html>